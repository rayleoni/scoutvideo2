<h1>Scout Reports</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Date</th>
    <th>Offer</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @scouts.each do |scout| %>
  <tr>
    <td><%= scout.name %></td>
    <td><%= scout.date %></td>
    <td><%= scout.offer %></td>
    <td><%= scout.email %></td>
    <td><%= link_to 'Show', scout %></td>
    <td><%= link_to 'Edit', edit_scout_path(scout) %></td>
    <td><%= link_to 'Destroy', scout, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Scout Someone', new_scout_path %>
